#include <stdio.h>
int is_prime(int num){
    int i;
    if (num%2 == 0 || num%3 == 0)
        return 1;
    else
        if (num % 2 ==0 )
            return 0;
    for
}
int main(){
    freoepn("input.txt", "r", stdin);

}
